#include "main.h"

/**
* _abs - checks for lowercase character
* @c: the character to check
* Return: n if n > is lowercase, c otherwise
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
